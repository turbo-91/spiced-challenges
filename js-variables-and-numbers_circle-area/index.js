radiusOfACircle = 25091991;
circleArea = Math.PI * Math.pow(radiusOfACircle, 2);
circumference = 2 * Math.PI * radiusOfACircle;

console.log(
  "For a circle with a radius of",
  radiusOfACircle,
  "units we can say that the area is approximately",
  circleArea,
  "square units and that the circumference is approximately",
  circumference,
  "units."
);
